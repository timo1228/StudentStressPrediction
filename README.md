For using KNN Search Model, please install chromadb, a vector database based on an in-memory HNSW graph for efficient ANN search \
For installation of Chromadb, simply run :
```
pip install chromadb
```